const baseURL = 'https://jsonplaceholder.typicode.com'

const urls = {
  users: `${baseURL}/users`,
  comments: `${baseURL}/comments`,
  posts: `${baseURL}/posts`,
  todos: `${baseURL}/todos`,
  albums: `${baseURL}/albums`
}

export {urls}
